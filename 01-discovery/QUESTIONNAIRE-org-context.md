# Questionnaire: Organization Context

**Instructions for you (the human):** Answer every question in this file before running `PROMPT-04-atlantis-on-bitbucket-dc.md`, `PROMPT-05-piaas-quality-gates.md`, or `PROMPT-16-secrets-integration.md`. The executing AI cannot infer these answers from AWS. Write your answers directly in this file under each question.

---

## Section A: Bitbucket Data Center

**A1.** What is the exact base URL of your Bitbucket Data Center instance (no trailing slash)?

> **Your answer:**

**A2.** What port does Bitbucket DC use for SSH (git clone via SSH)?

> **Your answer:** (default is 7999)

**A3.** What is the Bitbucket project key where the infrastructure repos will live?

> **Your answer:** (e.g. `INFRA`, `PLATFORM`, `OPS`)

**A4.** Do you have permission to create a Bitbucket service account (bot user) for Atlantis?

> **Your answer:** yes / no / need to ask IT

**A5.** What Bitbucket permissions does the Atlantis service account need?

> **Your answer:** (REPO_READ + REPO_WRITE on the infra repo is standard; confirm with your Bitbucket admin)

**A6.** Does your Bitbucket DC have a webhook allow-list or firewall rule that limits which IPs can send webhooks? If so, what IP range does Atlantis need to be allowed on?

> **Your answer:**

**A7.** Is there an existing Bitbucket personal access token (PAT) or HTTP access token mechanism available? Or does Bitbucket DC require Basic Auth?

> **Your answer:**

---

## Section B: piaas.yml Platform Pipeline

**B1.** What version of `piaas.yml` does your platform use? (Check with your platform team or look at existing `piaas.yml` files in other repos.)

> **Your answer:**

**B2.** What runner images are available in the pipeline? Specifically:
- Is there an image with Terraform pre-installed? If so, what image and version?
- Is there a general-purpose Linux image where we can install tools?

> **Your answer:**

**B3.** How does `piaas.yml` define pipeline triggers? Paste or describe the syntax for:
- Run on pull request open / update
- Run on push to `main`

> **Your answer:**

**B4.** How does `piaas.yml` expose environment variables to pipeline steps? (e.g. `env:` block, secrets vault, encrypted variables)

> **Your answer:**

**B5.** Does the pipeline runner have outbound internet access (to download tflint, trivy, checkov)? Or does it use an internal artifact registry?

> **Your answer:**

**B6.** Can you share a minimal working `piaas.yml` from another repo as an example? (Paste the content here or describe the structure.)

> **Your answer:**

**B7.** Is there a maximum pipeline runtime enforced? (e.g. 30 minutes — relevant since `terraform validate` with module downloads can be slow)

> **Your answer:**

---

## Section C: Secrets Management

**C1.** What secrets tool is already in use at the company?

> **Your answer:** (e.g. HashiCorp Vault, AWS Secrets Manager, CyberArk, 1Password Secrets Automation, Doppler, Azure Key Vault, Bitwarden Secrets Manager)

**C2.** Is there a Terraform provider for this tool? If yes, what is the provider source and typical version used?

> **Your answer:** (e.g. `hashicorp/vault` 4.8.0, `hashicorp/aws` for Secrets Manager)

**C3.** How does the secrets tool authenticate when called from Terraform running in Atlantis on EKS? Options:
- IRSA (if the tool is in AWS — e.g. Secrets Manager, Parameter Store)
- Vault Kubernetes auth method
- Token/AppRole (static credentials — discouraged)
- Other

> **Your answer:**

**C4.** Where will database master passwords (e.g. DocumentDB master user) be stored after Terraform creates the resource?

> **Your answer:** (e.g. "auto-generated by random_password, written to Vault at path `secret/data/<org>/<env>/docdb/master`")

**C5.** Are there any secrets Terraform needs to read (not write) at plan time? For example, certificates, API keys for providers (Datadog, etc.)?

> **Your answer:**

**C6.** Does the secrets tool have a concept of "environments" or "paths" that align with staging vs. production?

> **Your answer:**

---

## Section D: AWS SSO and Identity

**D1.** Does the company use AWS IAM Identity Center (SSO) for human access to AWS accounts?

> **Your answer:** yes / no

**D2.** If yes — who manages the SSO permission sets and account assignments? The platform team? A separate identity team? Another tool?

> **Your answer:**

**D3.** How do engineers currently assume roles to run AWS CLI commands locally (for debugging, not for Terraform apply)?

> **Your answer:** (e.g. `aws sso login`, named profiles in `~/.aws/config`, assume-role from a bastion)

**D4.** Is there a break-glass / emergency admin role in each account for incidents?

> **Your answer:**

**D5.** Does the platform team already have an automation IAM role in each account that Terraform should not conflict with?

> **Your answer:** (Name the role if known — this is critical for the `10-identity` layer to not accidentally delete it)

---

## Section E: Team and Process

**E1.** How many engineers will be making Terraform changes simultaneously?

> **Your answer:** (affects Atlantis concurrency settings)

**E2.** What is the PR approval policy?
- Minimum number of reviewers required before `atlantis apply` is allowed?
- Is self-approval allowed?

> **Your answer:**

**E3.** Is there a change management process (ITSM tickets, change windows) that Terraform applies must comply with?

> **Your answer:**

**E4.** Do you want Atlantis to auto-apply on PR merge (no separate `atlantis apply` comment needed)? Or should apply always require an explicit comment?

> **Your answer:** (Recommendation: explicit comment. Auto-apply is risky for infrastructure.)

**E5.** Should production Terraform changes require a different/higher approval level than staging?

> **Your answer:**

---

## Section F: Network and Connectivity

**F1.** Is Atlantis (running on EKS staging) expected to manage resources in the production account? If so, how does it reach the production account — VPC peering? TGW? Public endpoints?

> **Your answer:**

**F2.** Are there any network egress restrictions that would block Terraform from downloading modules from Bitbucket DC or the Terraform Registry?

> **Your answer:**

**F3.** Is there a private Terraform module registry or proxy (e.g. Artifactory, Nexus) in use, or is the public `registry.terraform.io` reachable from the pipeline runner?

> **Your answer:**

**F4.** Are there any VPN or Direct Connect connections between the AWS accounts and on-premises infrastructure that must be represented in Terraform?

> **Your answer:**

---

## Section G: Platform Automation (piaas EKS provisioning)

**G1.** What platform automation tool provisioned the EKS cluster? (e.g. an internal tool, Crossplane, a custom Helm chart, a company-built CLI)

> **Your answer:**

**G2.** Where is the Terraform state file that the platform generated for EKS? (S3 bucket name, key, region)

> **Your answer:**

**G3.** Does the platform automation continue to apply changes to the EKS cluster after provisioning, or was it a one-time run?

> **Your answer:** (This determines whether we can fully adopt EKS in our Terraform or must use `lifecycle { ignore_changes }`)

**G4.** Can you get read access to the platform automation's source code (the Terraform/scripts that provisioned EKS)?

> **Your answer:**

---

## What to do after answering

1. Save this file with your answers.
2. Copy the Bitbucket and piaas.yml answers into `00-standards/fill-in-the-blanks.local.md` under the corresponding placeholders.
3. Share this file with the executing AI when running:
   - `PROMPT-04-atlantis-on-bitbucket-dc.md` (needs Sections A, D, E)
   - `PROMPT-05-piaas-quality-gates.md` (needs Sections B, F)
   - `PROMPT-16-secrets-integration.md` (needs Section C)
   - `QUESTIONNAIRE-20-eks-handover.md` (needs Sections G, D)
